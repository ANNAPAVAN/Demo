function chech(array)
{
    const counts={}
    for(const item of array)
    {
        if(counts[item])
        {
            counts[item] = counts[item]+1
        }
        else
        {
            counts[item]=1
        }

        let mfitem = null;
        let maxx=0;
        
    }
}
const arr={}